namespace Shared.Enums;

/// <summary>
/// Returns a status based on given result.
/// </summary>
public enum ServiceStatus
{
    FAILED = 0,
    SUCCESSED = 1,
    ALREADY_EXISTS = 2,
    NOT_FOUND = 3,
    UPDATED = 4,
    DELEETED = 5,
}
